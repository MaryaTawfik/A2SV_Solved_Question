# didvid to k
#divid=sum(max(I)-min(i))
#min cost
# to be minimum we should take the largest one 
#1 3 3 7
n,k=map(int,input().split())
arr=list(map(int,input().split()))
diff=[]
for i in range(1,n):
    diff.append(arr[i]-arr[i-1])
diff.sort(reverse=True)
total=arr[-1]-arr[0]

x=sum(diff[:k-1])
print(total-x)
